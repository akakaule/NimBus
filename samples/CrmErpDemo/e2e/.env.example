# npm run test:live discovers URLs from the running CRM/ERP Aspire AppHost.
# These defaults are for direct Playwright runs against the standard pinned ports.
# Set E2E__Key in the shell to the random key used to start the E2E AppHost.
# See README.md for the opt-in test profile and isolated startup command.

# CRM REST API (used to create/read/delete CRM-side entities)
CRM_API_URL=http://localhost:5080

# ERP REST API (used to create/read/delete ERP-side entities AND toggle failure modes)
ERP_API_URL=http://localhost:5090

# NimBus management WebApp (used to view endpoint status and resubmit failed messages)
NIMBUS_OPS_URL=http://localhost:28376

# Optional: how long to wait for cross-system propagation (ms)
PROPAGATION_TIMEOUT_MS=60000

# Optional: how long to wait for messages to land in NimBus admin as Failed (ms)
FAILED_MESSAGE_TIMEOUT_MS=45000

# ── Demo film voice-over (only needed for `npm run demo:voice`) ──────────────
# Put the key in .env.local, not here — .env.local is gitignored.
# Run `npm run demo:voices` to list the voices and models your account can use.
# ELEVENLABS_API_KEY=sk_...
# ELEVENLABS_VOICE_ID=onwK4e9ZLuTAKqWW03F9
# ELEVENLABS_MODEL_ID=eleven_multilingual_v2

# Optional: shift every narration cue by this many ms when muxing. Playwright
# starts recording a moment before the test body, so the default nudges the audio
# later. Raise it if the voice runs ahead of the picture, lower it if it lags.
# NARRATION_OFFSET_MS=250
