# Implementation plan

1. Add failing Node tests for report status aggregation, retry handling, incomplete
   runs, global errors and safe HTML output.
2. Implement a standalone report generator driven by the Playwright catalog and
   results, including run provenance and searchable scenario results.
3. Configure CI reporters and npm scripts; add the manual/nightly Aspire workflow,
   artifact generation, bounded diagnostic collection and unconditional cleanup.
4. Document run instructions and distinguish historical coverage from current runs.
5. Verify report tests, TypeScript, actual Playwright catalog/report compatibility,
   HTML rendering and workflow lint. A hosted execution requires publishing the
   workflow; report that separately from local validation.

## Local verification

- Seven Node checks passed, including an actual Playwright fixture execution with
  passing, failing, flaky and skipped outcomes matched to its discovered catalog.
- TypeScript and actionlint 1.7.12 passed (actionlint's optional ShellCheck disabled
  because that executable is not installed locally); `git diff --check` passed.
- The actual 84-scenario catalog with missing results produced a nonzero exit and
  84 unexecuted scenarios. Chromium verified outcome/search filters, literal markup
  input and 390px/1440px layouts; both screenshots were inspected.
- No full demo E2E run or hosted Linux execution was performed for this workflow
  change. The first GitHub run remains a separate validation step after publication.
